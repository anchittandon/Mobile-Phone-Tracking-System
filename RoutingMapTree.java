public class RoutingMapTree{
	Exchange root;
	RoutingMapTree(){
		root = new Exchange(0);
	}
	public Boolean containsNode(Exchange node){
		if(root = node){
			return true;
		}
		int n = root.numChildren();
		for(int i=0;i<n;i++){
			if()
		}
	}

}
